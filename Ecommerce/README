# Variable Description

This document provides descriptions for the variables used in the dataset. 

| **Table Name**                  | **Column Name**                   | **Data Type** | **Description**                                                                                     |
|--------------------------------|-----------------------------------|---------------|-----------------------------------------------------------------------------------------------------|
| `product_category_name_translation` | `product_category_name`           | VARCHAR       | The name of the product category in the original language.                                          |
| `product_category_name_translation` | `product_category_name_english`   | VARCHAR       | The name of the product category translated into English.                                          |
| `olist_sellers_dataset`           | `seller_id`                       | VARCHAR       | Unique identifier for each seller.                                                                 |
| `olist_sellers_dataset`           | `seller_zip_code_prefix`          | INTEGER        | The zip code prefix for the seller's location.                                                      |
| `olist_sellers_dataset`           | `seller_city`                     | VARCHAR       | The city where the seller is located.                                                               |
| `olist_sellers_dataset`           | `seller_state`                    | VARCHAR       | The state where the seller is located.                                                              |
| `olist_products_dataset`          | `product_id`                      | VARCHAR       | Unique identifier for each product.                                                                 |
| `olist_products_dataset`          | `product_category_name`           | VARCHAR       | The name of the product category.                                                                   |
| `olist_products_dataset`          | `product_name_length`             | INTEGER        | The length of the product name in characters.                                                       |
| `olist_products_dataset`          | `product_description_length`      | INTEGER        | The length of the product description in characters.                                                |
| `olist_products_dataset`          | `product_photos_qty`              | INTEGER        | The number of photos provided for the product.                                                      |
| `olist_products_dataset`          | `product_weight_g`                | INTEGER        | The weight of the product in grams.                                                                 |
| `olist_products_dataset`          | `product_length_cm`               | INTEGER        | The length of the product in centimeters.                                                           |
| `olist_products_dataset`          | `product_height_cm`               | INTEGER        | The height of the product in centimeters.                                                           |
| `olist_products_dataset`          | `product_width_cm`                | INTEGER        | The width of the product in centimeters.                                                            |
| `olist_orders_dataset`            | `order_id`                        | VARCHAR       | Unique identifier for each order.                                                                   |
| `olist_orders_dataset`            | `customer_id`                     | VARCHAR       | Unique identifier for each customer.                                                                |
| `olist_orders_dataset`            | `order_status`                    | VARCHAR       | The current status of the order (e.g., delivered, canceled).                                        |
| `olist_orders_dataset`            | `order_purchase_timestamp`        | TIMESTAMP     | The timestamp when the order was placed.                                                            |
| `olist_orders_dataset`            | `order_approved_at`               | TIMESTAMP     | The timestamp when the order was approved.                                                          |
| `olist_orders_dataset`            | `order_delivered_carrier_date`    | TIMESTAMP     | The timestamp when the order was delivered to the carrier.                                          |
| `olist_orders_dataset`            | `order_delivered_customer_date`   | TIMESTAMP     | The timestamp when the order was delivered to the customer.                                         |
| `olist_orders_dataset`            | `order_estimated_delivery_date`   | TIMESTAMP     | The estimated date for the order to be delivered.                                                   |
| `olist_order_payments_dataset`    | `order_id`                        | VARCHAR       | Unique identifier for each order (same as in `olist_orders_dataset`).                              |
| `olist_order_payments_dataset`    | `payment_sequential`              | INTEGER        | Sequential number of the payment.                                                                   |
| `olist_order_payments_dataset`    | `payment_type`                    | VARCHAR       | The type of payment used (e.g., credit card, bank transfer).                                        |
| `olist_order_payments_dataset`    | `payment_installments`            | INTEGER        | The number of installments for the payment.                                                         |
| `olist_order_payments_dataset`    | `payment_value`                   | REAL          | The value of the payment.                                                                           |
| `olist_order_items_dataset`       | `order_id`                        | VARCHAR       | Unique identifier for each order item (same as in `olist_orders_dataset`).                         |
| `olist_order_items_dataset`       | `order_item_id`                   | INTEGER        | Unique identifier for each item within an order.                                                     |
| `olist_order_items_dataset`       | `product_id`                      | VARCHAR       | Unique identifier for each product (same as in `olist_products_dataset`).                         |
| `olist_order_items_dataset`       | `seller_id`                       | VARCHAR       | Unique identifier for each seller (same as in `olist_sellers_dataset`).                           |
| `olist_order_items_dataset`       | `shipping_limit_date`            | TIMESTAMP     | The limit date for shipping the order.                                                              |
| `olist_order_items_dataset`       | `price`                           | REAL          | The price of the order item.                                                                        |
| `olist_order_items_dataset`       | `freight_value`                   | REAL          | The value of the freight charge for the order item.                                                 |
| `olist_geolocation_dataset`        | `geolocation_zip_code_prefix`     | INTEGER        | Zip code prefix for the geographical location.                                                      |
| `olist_geolocation_dataset`        | `geolocation_lat`                 | REAL          | Latitude of the geographical location.                                                              |
| `olist_geolocation_dataset`        | `geolocation_lng`                 | REAL          | Longitude of the geographical location.                                                             |
| `olist_geolocation_dataset`        | `geolocation_city`                | VARCHAR       | City of the geographical location.                                                                   |
| `olist_geolocation_dataset`        | `geolocation_state`               | VARCHAR       | State of the geographical location.                                                                  |
| `olist_customers_dataset`          | `customer_id`                     | VARCHAR       | Unique identifier for each customer (same as in `olist_orders_dataset`).                          |
| `olist_customers_dataset`          | `customer_unique_id`              | VARCHAR       | A unique identifier for each customer across all orders.                                            |
| `olist_customers_dataset`          | `customer_zip_code_prefix`        | INTEGER        | Zip code prefix for the customer's location.                                                         |
| `olist_customers_dataset`          | `customer_city`                   | VARCHAR       | City of the customer's location.                                                                    |
| `olist_customers_dataset`          | `customer_state`                  | VARCHAR       | State of the customer's location.                                                                   |
